services:
  visit_theme.twig.config:
    class: Drupal\Core\Template\TwigEnvironment
    tags:
      - { name: twig.extension }
    arguments:
      - "@twig"
      - { debug: true, auto_reload: true, cache: false }
